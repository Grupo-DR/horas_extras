import React from 'react';import { formatCurrencyWithZero } from '../utils/calculations';import { X } from 'lucide-react';// Custom Tooltip para o gráfico de evolução diáriaexport const CustomDailyTooltip = ({ active, payload }: any) => {    if (!active || !payload || !payload[0]) return null;    const data = payload[0].payload;    const equipments = data.equipments || [];    const totalReal = data.real || 0;    const totalPlan = data.plan || 0;    const difference = totalReal - totalPlan;    return (        <div className="bg-white p-4 rounded-xl shadow-2xl border-2 border-slate-200 min-w-[300px]">            <p className="font-black text-sm text-slate-900 mb-3 border-b pb-2">{data.name}</p>            <div className="space-y-2 mb-3">                <div className="flex justify-between items-center">                    <span className="text-xs text-slate-500">Planejado:</span>                    <span className="text-sm font-bold text-slate-700">{formatCurrencyWithZero(totalPlan)}</span>                </div>                <div className="flex justify-between items-center">                    <span className="text-xs text-slate-500">Realizado:</span>                    <span className="text-sm font-bold text-indigo-600">{formatCurrencyWithZero(totalReal)}</span>                </div>                <div className="flex justify-between items-center pt-2 border-t">                    <span className="text-xs font-bold text-slate-700">Diferença:</span>                    <span className={`text-sm font-bold ${difference >= 0 ? 'text-emerald-600' : 'text-red-600'}`}>                        {difference >= 0 ? '+' : ''}{formatCurrencyWithZero(difference)}                    </span>                </div>            </div>            {equipments.length > 0 && (                <div className="border-t pt-3">                    <p className="text-[10px] font-bold text-slate-400 uppercase mb-2">Equipamentos ({equipments.length})</p>                    <div className="max-h-40 overflow-y-auto space-y-1">                        {equipments.map((eq: any, i: number) => (                            <div key={i} className="flex justify-between items-center text-xs bg-slate-50 p-2 rounded">                                <span className="font-bold text-slate-700">{eq.frota}</span>                                <div className="flex gap-2">                                    <span className="text-slate-500">{formatCurrencyWithZero(eq.value)}</span>                                    {eq.planned > 0 && (                                        <span className="text-slate-400">({formatCurrencyWithZero(eq.planned)})</span>                                    )}                                </div>                            </div>                        ))}                    </div>                </div>            )}        </div>    );};// Modal para detalhes de categoria (Pareto)interface CategoryDetailModalProps {    category: any;    onClose: () => void;    type: 'revenue' | 'idle';}export const CategoryDetailModal: React.FC<CategoryDetailModalProps> = ({ category, onClose, type }) => {    if (!category) return null;    return (        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 backdrop-blur-sm" onClick={onClose}>            <div className="bg-white rounded-3xl shadow-2xl p-8 max-w-2xl w-full mx-4 max-h-[80vh] overflow-y-auto" onClick={(e) => e.stopPropagation()}>                <div className="flex justify-between items-center mb-6">                    <div>                        <h3 className="text-xl font-black text-slate-900">{category.name || category.category}</h3>                        <p className="text-sm text-slate-500 mt-1">                            {type === 'revenue' ? 'Faturamento por Equipamento' : 'Horas Improdutivas por Equipamento'}                        </p>                    </div>                    <button onClick={onClose} className="p-2 hover:bg-slate-100 rounded-full transition-colors">                        <X className="w-5 h-5 text-slate-500" />                    </button>                </div>                <div className="bg-slate-50 p-4 rounded-xl mb-6">                    <div className="grid grid-cols-3 gap-4">                        <div>                            <p className="text-[10px] font-bold text-slate-400 uppercase">Planejado</p>                            <p className="text-lg font-black text-slate-700">{formatCurrencyWithZero(category.planned || 0)}</p>                        </div>                        <div>                            <p className="text-[10px] font-bold text-slate-400 uppercase">{type === 'idle' ? 'Improdutivo' : 'Realizado'}</p>                            <p className="text-lg font-black text-indigo-600">{formatCurrencyWithZero(category.actual || category.idle || 0)}</p>                        </div>                        <div>                            <p className="text-[10px] font-bold text-slate-400 uppercase">Diferença</p>                            <p className={`text-lg font-black ${((category.actual || category.idle || 0) - (category.planned || 0)) >= 0 ? 'text-emerald-600' : 'text-red-600'}`}>                                {((category.actual || category.idle || 0) - (category.planned || 0)) >= 0 ? '+' : ''}{formatCurrencyWithZero((category.actual || category.idle || 0) - (category.planned || 0))}                            </p>                        </div>                    </div>                </div>                <div className="space-y-2">                    <p className="text-xs font-bold text-slate-500 uppercase mb-3">Equipamentos</p>                    {category.equipmentDetails && category.equipmentDetails.length > 0 ? (                        <div className="space-y-2">                            {category.equipmentDetails.map((eq: any, i: number) => (                                <div key={i} className="bg-white border border-slate-200 p-4 rounded-xl hover:shadow-md transition-shadow">                                    {type === 'idle' ? (                                        <>                                            <div className="flex justify-between items-center">                                                <span className="font-bold text-slate-900">{eq.frota}</span>                                                <span className="text-sm font-bold text-red-600">{formatCurrencyWithZero(eq.idle)}</span>                                            </div>                                            <div className="mt-2">                                                <p className="text-[9px] text-slate-400 uppercase">Valor Improdutivo</p>                                                <p className="text-sm font-bold text-red-600">{formatCurrencyWithZero(eq.idle)}</p>                                            </div>                                        </>                                    ) : (                                        <>                                            <div className="flex justify-between items-center">                                                <span className="font-bold text-slate-900">{eq.frota}</span>                                                <span className={`text-sm font-bold ${eq.difference >= 0 ? 'text-emerald-600' : 'text-red-600'}`}>                                                    {eq.difference >= 0 ? '+' : ''}{formatCurrencyWithZero(eq.difference)}                                                </span>                                            </div>                                            <div className="grid grid-cols-2 gap-4 mt-2">                                                <div>                                                    <p className="text-[9px] text-slate-400 uppercase">Planejado</p>                                                    <p className="text-sm font-bold text-slate-600">{formatCurrencyWithZero(eq.planned)}</p>                                                </div>                                                <div>                                                    <p className="text-[9px] text-slate-400 uppercase">Realizado</p>                                                    <p className="text-sm font-bold text-indigo-600">{formatCurrencyWithZero(eq.actual)}</p>                                                </div>                                            </div>                                        </>                                    )}                                </div>                            ))}                        </div>                    ) : (                        <p className="text-sm text-slate-400 italic">Nenhum equipamento encontrado</p>                    )}                </div>            </div>        </div>    );};// Modal para detalhes de um dia específicointerface DayDetailModalProps {    day: any;    onClose: () => void;}export const DayDetailModal: React.FC<DayDetailModalProps> = ({ day, onClose }) => {    if (!day) return null;    const equipments = day.equipments || [];    const totalReal = day.real || 0;    const totalPlan = day.plan || 0;    const difference = totalReal - totalPlan;    return (        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 backdrop-blur-sm" onClick={onClose}>            <div className="bg-white rounded-3xl shadow-2xl p-8 max-w-2xl w-full mx-4 max-h-[80vh] overflow-y-auto" onClick={(e) => e.stopPropagation()}>                <div className="flex justify-between items-center mb-6">                    <div>                        <h3 className="text-xl font-black text-slate-900">{day.name}</h3>                        <p className="text-sm text-slate-500 mt-1">Equipamentos do Dia</p>                    </div>                    <button onClick={onClose} className="p-2 hover:bg-slate-100 rounded-full transition-colors">                        <X className="w-5 h-5 text-slate-500" />                    </button>                </div>                <div className="bg-slate-50 p-4 rounded-xl mb-6">                    <div className="grid grid-cols-3 gap-4">                        <div>                            <p className="text-[10px] font-bold text-slate-400 uppercase">Planejado</p>                            <p className="text-lg font-black text-slate-700">{formatCurrencyWithZero(totalPlan)}</p>                        </div>                        <div>                            <p className="text-[10px] font-bold text-slate-400 uppercase">Realizado</p>                            <p className="text-lg font-black text-indigo-600">{formatCurrencyWithZero(totalReal)}</p>                        </div>                        <div>                            <p className="text-[10px] font-bold text-slate-400 uppercase">Diferença</p>                            <p className={`text-lg font-black ${difference >= 0 ? 'text-emerald-600' : 'text-red-600'}`}>                                {difference >= 0 ? '+' : ''}{formatCurrencyWithZero(difference)}                            </p>                        </div>                    </div>                </div>                <div className="space-y-2">                    <p className="text-xs font-bold text-slate-500 uppercase mb-3">Equipamentos ({equipments.length})</p>                    {equipments.length > 0 ? (                        <div className="space-y-2">                            {equipments.map((eq: any, i: number) => {                                const eqDiff = eq.value - (eq.planned || 0);                                return (                                    <div key={i} className="bg-white border border-slate-200 p-4 rounded-xl hover:shadow-md transition-shadow">                                        <div className="flex justify-between items-center">                                            <span className="font-bold text-slate-900">{eq.frota}</span>                                            <span className={`text-sm font-bold ${eqDiff >= 0 ? 'text-emerald-600' : 'text-red-600'}`}>                                                {eqDiff >= 0 ? '+' : ''}{formatCurrencyWithZero(eqDiff)}                                            </span>                                        </div>                                        <div className="grid grid-cols-2 gap-4 mt-2">                                            <div>                                                <p className="text-[9px] text-slate-400 uppercase">Planejado</p>                                                <p className="text-sm font-bold text-slate-600">{formatCurrencyWithZero(eq.planned || 0)}</p>                                            </div>                                            <div>                                                <p className="text-[9px] text-slate-400 uppercase">Realizado</p>                                                <p className="text-sm font-bold text-indigo-600">{formatCurrencyWithZero(eq.value)}</p>                                            </div>                                        </div>                                    </div>                                );                            })}                        </div>                    ) : (                        <p className="text-sm text-slate-400 italic">Nenhum equipamento encontrado</p>                    )}                </div>            </div>        </div>    );};